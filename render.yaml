databases:
  - name: chendb
    plan: free
    databaseName: chen
    user: yulian

services:
  - type: web
    plan: free
    name: chen
    runtime: python
    buildCommand: './build.sh'
    startCommand: 'python -m gunicorn chen.asgi:application -k uvicorn.workers.UvicornWorker'
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: chendb
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4